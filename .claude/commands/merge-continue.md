1. Switch to the default branch (usually `pu`, `main`, or `master` - check with git status or git branch)
2. Pull the latest changes from origin with `git pull`
3. Verify the working tree is clean with `git status`
4. Confirm that we're on the default branch and up to date
5. Ask the user what they'd like to work on next
